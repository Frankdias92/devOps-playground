name: CI

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: 'Build and Push'
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Generate SHA
      id: generate_sha
      run: |
        SHA=$(echo $GITHUB_SHA | head -c7)
        echo "sha=$SHA" >> $GITHUB_OUTPUT

    - name: Login into the container registry
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push
      uses: docker/build-push-action@v6
      with:
        context: .
        push: true
        tags: |
          frankdias92/nlw.journey.api:${{ steps.generate_sha.outputs.sha }}
          frankdias92/nlw.journey.api:latest

#    - name: Build Docker image
#      run: docker build -t Frankdias92/nlw.journey.api:${{ steps.generate_sha.outputs.sha }} .

 #   - name: Push image
 #     run: |
 #       docker push Frankdias92/nlw.journey.api:${{ steps.generate_sha.outputs.sha }}
 #       docker tag Frankdias92/nlw.journey.api:${{ steps.generate_sha.outputs.sha }} Frankdias92/nlw.journey.api:latest
 #       docker push Frankdias92/nlw.journey.api:latest
